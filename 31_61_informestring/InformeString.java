//Desenvolupa un programa que demani dos texts i un enter positiu i composi un petit informe amb el resultat de les funcions anteriors.


public class InformeString{
    public static void main (String[] args){
    
        
        System.out.println("Text principal?");
        String principal=Entrada.readLine();
        System.out.println("Text secundari?");
        String secundari=Entrada.readLine();
        System.out.println("Número positiu?");
        int numero= Integer.parseInt(Entrada.readLine());
        
        
        
        principal.length();
        principal.startsWith(String unAltre);
        principal.endsWith(String unAltre);
        principal.equals(String unAltre);
        principal.equalsIgnoreCase(String unAltre);
        principal.isBlank();
        principal.isEmpty();
        principal.charAt(int posicio);
        principal.concat(String unAltre);
        principal.repeat(int cops);
        principal.toUpperCase();
        principal.toLowerCase();
    
    }
}
